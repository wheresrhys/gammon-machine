
roll (board, currentPlayer) -> (board, currentPlayer, [dice])



move (board, currentPlayer, [dice], start, end) -> (board, currentPlayer, [dice], start, end)

checkVictory (board, currentPlayer) -> (board, currentPlayer, isVictorious)

changePlayer (board, currentPlayer) -> (draob, -currentPlayer)

draw (board, currentPlayer, [dice]) -> html

